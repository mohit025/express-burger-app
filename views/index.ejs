<!DOCTYPE html>
<html lang="en">
<%- include('./header.ejs') %>

  <body>

    <h1 class="p-2 mt-5">All Burger Order</h1>
    <div class="container">
      <div class="mt-5">
        <% if(orders.length>0) {%>
          <% orders.forEach(order=>{ %>
            <div class="row mt-5">
              <div class="col-md-2">
                <img src="../burger.jpg" width="150" alt="">
              </div>
              <div class="col-md-10">
                <h5>Name: <%=order.name %>
                </h5>
                <p>Burger Order: <%= order.burger %></p>
                    <p>Location: <%=order.location %></p>
                        <p>Order TIme: <%= order.createdAt %>
                        </p>
              </div>
              
            </div>
            <hr>
            <% }) %>
            <% }    else { %>
                <h4 class="text-center p-6">No orders till now!</h4>
                <% } %>
                  
      </div>
    </div>
  </body>
  <%- include('./footer.ejs') %>

</html>